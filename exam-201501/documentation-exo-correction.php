<?php

/*

1. Quelle est la description de la fonction `strtolower()` ?
   Voir la section « Référence des fonctions > Traitement du texte > Chaîne de caractères » de la documentation php

Renvoie une chaîne en minuscules

2. Quels sont les trois paramètres obligatoires de la fonction `str_replace()` ? Citez le nom de ces paramètres.
   Voir la section « Référence des fonctions > Traitement du texte > Chaîne de caractères » de la documentation php

`$search, $replace, $subject`

3. Quel type de données la fonction `explode()` est-elle censée renvoyer ?
   Voir la section « Référence des fonctions > Traitement du texte > Chaîne de caractères » de la documentation php

un tableau (`array`)

4. À partir de quelle version de php est-ce que la fonction `empty()` supporte les expressions ?
   Voir la section « Référence des fonctions > Extensions relatives aux variables et aux types > Fonctions de gestion des variables » de la documentation php

5.5.0

*/

